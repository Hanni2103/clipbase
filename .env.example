# 服务端口
PORT=3000

# SQLite 数据库文件路径（相对项目根目录）
DATABASE_PATH=./data/clipbase.db

# 分类大模型（OpenAI 兼容接口）。
# 留空 LLM_API_KEY 则使用内置 mock 分类器（无需联网/Key，便于本地联调）。
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=
LLM_MODEL=deepseek-chat

# 多模态大模型（图片 OCR 用，M3）。留空则图片分享标记为待处理。
VISION_MODEL=
